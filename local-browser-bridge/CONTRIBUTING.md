# Contributing

Keep the daemon local-only and preserve the extension as the final approval surface for browser websites. New browser powers must be capability-scoped, documented, and covered by a security review before they are exposed to websites.

Run `npm run check` and `npm test` before sending a change.
