# Changelog

## Unreleased

- Make the standalone daemon and extension the only active Session Manager bridge.
- Add CLI commands for windows, debugger targets, raw CDP, and allowlisted Chrome APIs.
- Keep the extension-owned page context menu and daemon-backed page values as the single active implementation.

## 0.1.0-alpha.1

- Initial standalone daemon, CLI, and Chrome extension.
- Token-based CLI and extension pairing.
- Extension-owned website authorization prompts and origin-scoped grants.
